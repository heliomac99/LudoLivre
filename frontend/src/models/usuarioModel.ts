export interface LoginModel {
  email: string;
  password: string;
}

export interface UpdateModel {
  nome: string;
  email: string;
  tipoUsuarioIds: []
}

export interface RegistroModel {
  nome: string;
  email: string;
  password: string;
}
